#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strlen - this pronts the length of a string
 * @s: is the argument
 * Return: zero if success
 */

int _strlen(char *s)
{
	int i;
	char st[i];
	*s = st[i];

	while (st[i] = '\n')
	{
		_putchar((i - 1) + '0');
		i++;
	}
	return (0);
}


