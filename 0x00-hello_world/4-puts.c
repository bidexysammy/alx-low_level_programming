#include <stdio.h>
#include <string.h>

/**
 * main - is the only function
 * description - this code prints strings
 * Return: 0 if exited properly, non-zero otherwise
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
