#include <stdio.h>
#include "main.h"

int a, b;

int sum(int a, int b)
{
	return (a + b);
}
int sub(int a, int b)
{
	return (a - b);
}
int mul(int a, int b)
{
	return (a * b);
}
int div(int a, int b)
{
	return (a / b);
}
int mol(int a, int b)
{
	return (a % b);
}
