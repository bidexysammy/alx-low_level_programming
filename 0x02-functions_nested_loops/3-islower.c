#include "main.h"
/**
 * _islower - checks for lowercase
 *@c:int c is the argument
 * Return: two if success
 */
int _islower(int c)
{
	int a, b;

	a = 1;
	b = 0;

	if (_islower(c))
	{
		_putchar(a + '0');
	}
	else
	{
		_putchar(b + '0');
	}
	_putchar('\n');
	return (2);
}


